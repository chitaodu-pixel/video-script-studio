# 架构说明

VideoScript Studio 一期采用分层结构：

- `domain`：项目、转写片段和替换规则等纯数据模型。
- `services`：环境检查、项目存储、媒体处理、转写、文本处理、替换、TTS 和导出。
- `workers`：把耗时任务放入 Qt 线程池，并通过信号返回进度和结果。
- `ui`：只负责交互和状态展示，不直接实现媒体或文本算法。

外部程序必须通过参数数组调用。项目 JSON 采用临时文件写入后原子替换。原始转写、清洗稿和改写稿分别保存，不互相覆盖。

